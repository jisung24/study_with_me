"use strict";

// 복사 방법은 타입에 따라서 나뉜다.
// - Deep Copy : 새로운 공간에 값만 복사해와서 원본과는 따로 노는 복사.
// - Shallow Copy : 주소값을 가져와서 원본과 공유하는 복사

{
  // 👉 원시 타입 : 깊은 복사
  // 👉 객체 타입 : 둘 다 지원 되지만 원래는 얕은 복사
}

{
    
}