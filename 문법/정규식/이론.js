"use strict";

// 목적
// 1. 패턴을 이용하여서 문자 검색 (search)
// 2. 문자를 대체(replace)
// 3. 문자 추출(extract)
// 🔴 성능은 매우 느리지만 짧은 문자열에서 사용하기 떄문에, 괜찮음🔴

// /슬래시를 열면 시작하고 /가 오면 끝난다.
// /와 / 사이가 패턴이 된다.

// 정규표현식을 이용해서 휴대폰 번호를 찾아보자!
//
