"use strict";
let my_string = "3 + 4";

let solution = (my_string) => {
  let splited = my_string.split(" ");
  // 이거 말고... split안에 정규식 써서 - +를 한 번에 받아주면 안 되나...?

  // for(let value of splited) {
  //     if(value === "+") {

  //     }
  // }
};
